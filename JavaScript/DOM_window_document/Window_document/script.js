/**
 * DOM -> DOCUMENT OBJECT MODEL
 *
 * Janela aberta no navegador que contem o objeto DOM.
 * Cada aba do navegador contem seu proprio objeto window.
 * Pode ser referenciado por window.
 *
 * WINDOW -> JANELA
 *
 * DOCUMENT
 * O objeto document representa o documento HTML aberto no navegador.
 * Pode ser referenciado por window.document ou somente document.
 *
 */

console.log(window);

console.log(document); // ao adicionar/interagir com efeitos, vamo interagir com o document

window.alert("Olá mundo");
